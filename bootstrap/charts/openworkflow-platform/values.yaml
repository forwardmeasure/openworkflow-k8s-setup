gcp:
  projectId: ""
externalSecrets:
  storeName: platform-secret-store
  provider: {}
rootDomain: example.invalid
authHostname: auth.example.invalid
gatewayIp: 192.0.2.10
cloudsqlPrivateIp: 10.0.0.10
acmeEmail: operator@example.invalid
acmeServer: https://acme-staging-v02.api.letsencrypt.org/directory
keycloakReplicas: 2
kafkaStorageSize: 100Gi
namespaces:
  identity: identity
  kafka: kafka
  actorEngine: openworkflow-actor-engine
  kafkaStreams: openworkflow-kafka-streams
gateway:
  namespace: istio-system
  name: platform-gateway
  listenerName: root-https
identity:
  realm: openworkflow
  publicClientId: openworkflow-public
kafka:
  clusterName: kafka-cluster
  version: 4.3.0
  metadataVersion: 4.3-IV0
  controllerReplicas: 3
  brokerReplicas: 3
secrets:
  keycloakDatabase: keycloak-database
  keycloakAdmin: keycloak-admin
  actorEngineDatabase: actor-engine-database
databases:
  actorEngine: openworkflow_actor_engine
databaseUsers:
  actorEngine: openworkflow_actor
